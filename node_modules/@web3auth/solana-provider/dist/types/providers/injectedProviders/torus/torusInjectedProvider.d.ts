import { BaseProvider, BaseProviderConfig, BaseProviderState } from "@web3auth/base-provider";
import { InjectedProvider } from "../interface";
export declare class TorusInjectedProvider extends BaseProvider<BaseProviderConfig, BaseProviderState, InjectedProvider> {
    constructor({ config, state }: {
        config: BaseProviderConfig;
        state?: BaseProviderState;
    });
    switchChain(_: {
        chainId: string;
    }): Promise<void>;
    setupProvider(injectedProvider: InjectedProvider): Promise<void>;
    protected lookupNetwork(): Promise<string>;
    private setupEngine;
    private handleInjectedProviderUpdate;
}
