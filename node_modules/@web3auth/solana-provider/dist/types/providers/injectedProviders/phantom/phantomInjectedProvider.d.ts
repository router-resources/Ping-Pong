import { BaseProvider, BaseProviderConfig, BaseProviderState } from "@web3auth/base-provider";
import { IPhantomWalletProvider } from "../../../interface";
export declare class PhantomInjectedProvider extends BaseProvider<BaseProviderConfig, BaseProviderState, IPhantomWalletProvider> {
    constructor({ config, state }: {
        config: BaseProviderConfig;
        state?: BaseProviderState;
    });
    switchChain(_: {
        chainId: string;
    }): Promise<void>;
    setupProvider(injectedProvider: IPhantomWalletProvider): Promise<void>;
    protected lookupNetwork(_: IPhantomWalletProvider): Promise<string>;
}
