import { FC } from 'react';
import { PayloadType } from './types';
declare const NotificationProvider: FC;
export declare const useNotification: () => (props: PayloadType) => void;
export default NotificationProvider;
